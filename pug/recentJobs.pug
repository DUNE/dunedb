extend default
include common.pug

      
block content
  h2 Recently completed jobs:

  table.table
    each test in tests
        tr
            td 
                a(href=`/job/${test._id}`) 
                    =moment(test.insertDate).format()
            td
               +short_user(test.user)
            td
                a(href=`/json/job/${test._id}`) JSON data


